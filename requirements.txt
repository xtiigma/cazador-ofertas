# Cazador de Ofertas — dependencias Python
# Instalación (entorno virtual recomendado, fuera del proyecto):
#   python3 -m venv ~/.venvs/cazador-ofertas
#   ~/.venvs/cazador-ofertas/bin/pip install -r requirements.txt
#
# Desde 2026-07-07 NINGÚN scraper usa navegador (Saga migró a requests el
# 04/07 e Inkafarma el 07/07): playwright/selenium ya no son necesarios.
# Siguen instalados en el venv actual por si hiciera falta volver atrás.

requests==2.31.0

# Etapa 5 (nube): cliente libSQL para replicar el historial en Turso. La mini PC
# puede correr sin él (analizador/conexion.py cae a sqlite3 local si no está);
# es obligatorio para que el ciclo corra en GitHub Actions contra Turso.
libsql==0.1.11
beautifulsoup4==4.12.3
